<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.movingstudio.secretstory">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application android:name="com.zeus.sdk.AresApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name">
        <!-- 全面屏适配 -->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.2" />
        <!-- 如果游戏有Application，设置游戏的代理Application，没有则不需要配置此项 -->
        <meta-data
            android:name="ZEUS_GAME_APPLICATION"
            android:value="com.zeus.sdk.test.GameProxyApplication" />
        <activity
            android:name="com.zeus.sdk.test.AnalyticsActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
        </activity>
        <!-- demo activity end -->
    </application>

</manifest>